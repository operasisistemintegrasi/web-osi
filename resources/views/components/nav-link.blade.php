@props(['active'=>false])
<a {{ $attributes }} class="{{ $active ? 'bg-white text-green-700' : 'text-white hover:bg-slate-200 hover:text-green-700' }} rounded-lg px-3 py-2 text-sm font-medium" aria-current="{{ $active ? 'page' : false }}">{{ $slot }}</a>